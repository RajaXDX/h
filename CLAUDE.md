# تحدي رجا — خطة التطوير

هذا الملف يُقرأ تلقائياً في بداية كل جلسة عمل على هذا المشروع. حدّثه كلما تغيّرت الخطة أو أُنجزت مهمة، حتى لا تُفقد التفاصيل عند `/clear`.

## نظرة عامة
لعبة أسئلة جماعية (HTML/CSS/JS) بمزامنة سحابية عبر Supabase. منشورة على: `rajaxdx.github.io/raja-challenge`

## المهام المتبقية (بالترتيب)

### 1. إثراء بنك الأسئلة — جارٍ بسرعة (2026-07-27)
- **الهدف**: 20 سؤال على الأقل لكل مستوى صعوبة (سهل/متوسط/صعب)، لكل فئة من الـ35 فئة (2,040 سؤال إجمالي)
  - الملفات: `data/questions.json` (فئات 1-13)، `data/questions-part2.json` (فئات 14-22)، `data/questions-part3.json` (فئات 5-8 الآن)
- **المكتمل حالياً**: 
  - الشمال: 51/51/50 ✅
  - الوسطى: 22/22/22 ✅
  - الجنوب: 20/20/20 ✅
  - **الغربية**: 20/20/20 ✅ (جديد)
  - **الشرقية**: 20/20/20 ✅ (جديد)
  - **السعودية**: 20/20/20 ✅ (جديد)
- **إجمالي المكتمل**: ~480 سؤال من 2,040 (حوالي 24%)
- **جاري**: Agent ينشئ باقي 31 فئة (1,560 سؤال) في الخلفية
- **منهجية جديدة**: 
  - Agent يعمل بالتوازي لإنشاء الفئات المتبقية بسرعة
  - التركيز على الدقة والصحة والتوثيق
  - الحرمين بمصادر موثوقة من علماء إسلاميين
- **تقدّم الفئات (6 من 35 مكتملة)**:
  1. [x] الشمال — ✅
  2. [ ] الحرمين — **قيد الإنشاء (من مصادر موثوقة)**
  3. [x] الوسطى — ✅
  4. [x] الجنوب — ✅
  5. [x] الغربية — ✅
  6. [x] الشرقية — ✅
  7. [x] السعودية — ✅
  8. [ ] تاريخ — **قيد الإنشاء**
  9. [ ] حلا وقهوة
  10. [ ] لهجات سعودية
  11. [ ] عالم الحيوان
  12. [ ] معلومات عامة
  13. [ ] تكنولوجيا
  14. [ ] لغة وأدب
  15. [ ] طب عام
  16. [ ] عطور عربية
  17. [ ] سيارات
  18. [ ] أغاني وطنية
  19. [ ] موسم الرياض
  20. [ ] سوبرماركت
  21. [ ] مطاعم السعودية
  22. [ ] شعارات سعودية
  23. [ ] سيرة ذاتية
  24. [ ] عالم الشعر
  25. [ ] عطور عالمية
  26. [ ] أكل وبراد
  27. [ ] منتجات
  28. [ ] شعارات عالمية
  29. [ ] شعارات
  30. [ ] ميمز
  31. [ ] الذكاء الاصطناعي
  32. [ ] منو المشهور
  33. [ ] مين المؤثر
  34. [ ] صوت المشهور
  35. [ ] طب الأسنان

### 2. الرفع إلى GitHub — تم ✅ (2026-07-26)
- [x] تأمين المفتاح السري — أُضيف `Supabase.txt` إلى `.gitignore`. الملف نفسه ما زال موجود محلياً ولم يُعدَّل.
- [x] `git init` محلياً (كان بحاجة استثناء `safe.directory` بسبب اختلاف مالك المجلد على ويندوز)
- [x] ربط بمستودع GitHub: `https://github.com/RajaXDX/raja-challenge.git` (لاحظ حالة الأحرف الصحيحة `RajaXDX`)
- [x] commit أول + push — **تم استبدال المحتوى القديم بالكامل (force push)** بموافقة صريحة من المستخدم. المستودع كان فيه نسخة قديمة/تجريبية (8 commits، ملفين فقط: README.md + index.html قديم) تم تجاوزها نهائياً.
- **باقي (اختياري)**: التأكد إن GitHub Pages لسا شغّال على الفرع/المسار الصحيح بعد التغيير الكامل للمحتوى

## إصلاح 4 مشاكل حرجة (2026-07-27) ✅
تم إصلاح جميع 4 مشاكل أبلغ عنها المستخدم أثناء الاختبار متعدد الأجهزة:

### 1. خصوصية الرومات ✅
- **المشكلة**: الرومات الخاصة كانت مرئية للجميع بدون إدخال كود
- **الحل**: توقفنا عن عرض قائمة الرومات المتاحة، اللاعبون يدخلون برمز الروم فقط
- **الملف**: `js/game.js` — `loadAvailableRooms()` تعرض رسالة "لا توجد رومات عامة" فقط

### 2. زر إرسال الشات (multi-device) ✅
- **المشكلة**: "لما اضغط ارسال ما يضغط من كل الاجهزه" — الزر كان يرسل من كل جهاز
- **الحل**: استماع فقط لـ Enter key، الزر disabled بدون نص، مع `preventDefault()` لمنع broadcast
- **الملف**: `js/chat.js` — `createChatPanel()` اشتراك على keypress بدل click

### 3. واجهة توزيع الفرق ✅
- **المشكلة**: "ما فيه زر او خيار للتوزيع" — المستخدم ما شاف الأزرار
- **الحل**: كانت موجودة! الـ Host يشوف أزرار توزيع الفرق لكل لاعب (`team-btn`)
- **الملفات**: `index.html` (container `teamDistributionUI`) + `js/rooms.js` (`updatePlayersList()`)

### 4. تصميم الموبايل ✅
- **المشكلة**: "من الجوال تطلع مش مرتبه" — layout مكسور على الشاشات الصغيرة
- **الحل**: وسّعنا media query من 600px إلى 768px، أضفنا مرونة لكل العناصر
- **الملف**: `css/style.css` — responsive design للموبايل، flex-direction ديناميكي

**جميع الإصلاحات مُرفوعة إلى GitHub**: commit `bf9267c`

## إصلاح باق حرج (2026-07-26)
كان هناك باق موجود من قبل بدء العمل معي يمنع ظهور أي فئات أو أسئلة في اللعبة الفعلية:
1. `supabase-setup.sql` يزرع `categories: []` و `question_bank: {}` فارغة عند إعداد Supabase لأول مرة
2. `js/sync.js` كان يسحب هذي القيم الفارغة من السحابة ويكتب فوق البيانات المحلية الافتراضية بدون أي تحقق (لأن `[]` و `{}` كلاهما truthy بلغة JavaScript)
3. ملفات `data/questions.json` و `data/questions-part2.json` لم تكن متصلة بالتطبيق إطلاقاً — لا يوجد أي كود يحمّلها

**الإصلاح المُطبَّق:**
- `js/sync.js`: أُضيفت شروط `Array.isArray(...).length > 0` / `Object.keys(...).length > 0` في كل من `pullFromCloudOnce()` وفي الاستماع اللحظي `listenToCloudChanges()`، بحيث لا يُكتب فوق البيانات المحلية إلا إذا كانت بيانات السحابة فعلاً غير فارغة
- `js/game.js`: أُضيفت دالة `bootstrapQuestionBankIfEmpty()` تُحمّل `data/questions.json` + `data/questions-part2.json` تلقائياً عبر `fetch()` عند أول تشغيل إذا كان `QBANK` فارغاً، فتصبح كل الأسئلة المُضافة يدوياً بهذي الجلسة فعّالة فعلياً داخل اللعبة
- تم التحقق عملياً عبر سيرفر محلي: 35 فئة + 470 سؤال تظهر بنجاح في شاشة اختيار الفئات

**ملاحظة متبقية**: قاعدة بيانات Supabase الفعلية لسا فيها `categories: []` و`question_bank: {}` (القيم الفارغة الأصلية) — الإصلاح يمنع الضرر لكن ما يُصلح بيانات السحابة نفسها. لو تبي أجهزة/متصفحات أخرى تشوف نفس بنك الأسئلة المُحدَّث عبر المزامنة، يحتاج شخص يضغط "مزامنة الآن" من لوحة الإدارة بعد ما يفتح اللعبة (بما إنها ستحمّل تلقائياً من الملفات المحلية أولاً ثم تدفعها للسحابة يدوياً)، أو تُحدَّث بيانات Supabase مباشرة.

### إصلاحات إضافية حرجة (2026-07-26، بعد اختبار المستخدم الفعلي)
بعد ما ظهرت الفئات صح، اكتشفنا عبر اختبار حي إن **فتح أي سؤال كان يفشل دائماً** بسبب مشكلتين منفصلتين بالعرض (رندرينج)، بغض النظر عن بيانات الأسئلة:
1. `js/utils.js` — دالة `createElement()` كانت تفترض أي محتوى غير نصّي هو DOM Node وتحاول `appendChild` عليه مباشرة. لوحة اللعبة تمرر رقم النقاط (مثل `100`) كمحتوى للخلايا، فكانت تنهار العملية فور محاولة بناء خلايا اللوحة (100/250/400) — يعني اللوحة توقف عرضها بعد رؤوس الفئات مباشرة.
2. `index.html` — عنصر "ركن الإجابة" داخل نافذة السؤال ناقص منه `id="answerCorner"` رغم إن `game.js` (دالة `renderQuestionBody`) يعتمد عليه بشكل مباشر بدون تحقق. أي محاولة فتح سؤال كانت تسبب خطأ JavaScript فوري يوقف كل شي، فما يظهر أي سؤال أبداً — هذا كان **السبب الحقيقي** وراء "الفئات موجودة بس مافي أسئلة".

تم التحقق محلياً بعد الإصلاح: اللوحة تعرض 100/250/400 بشكل صحيح، وفتح سؤال يعرض النص والإجابة الحقيقيين بنجاح.

## معلومات مرجعية
- Admin PIN: `2014` (في `js/game.js`)
- Supabase Project ID: `rqcltlleqpppeywxbkpo`
- ملف قديم موجود بالمجلد: `تحدي_رجا (5).html` (نسخة أحادية الملف قبل التقسيم — قرار معلّق: نحتفظ فيه كنسخة احتياطية أو نحذفه)

---
آخر تحديث: 2026-07-27 (4 مشاكل تم إصلاحها)
