# مخمخ رجا — خطة التطوير

هذا الملف يُقرأ تلقائياً في بداية كل جلسة عمل على هذا المشروع. حدّثه كلما تغيّرت الخطة أو أُنجزت مهمة، حتى لا تُفقد التفاصيل عند `/clear`.

## نظرة عامة
لعبة أسئلة جماعية (HTML/CSS/JS) بمزامنة سحابية عبر Supabase. منشورة على: `rajaxdx.github.io/raja-challenge`

## المهام المتبقية (بالترتيب)

### 1. إثراء بنك الأسئلة — لم يبدأ
- **الوضع الحالي**: 35 فئة، كل فئة فيها سؤالين فقط لكل مستوى (سهل/متوسط/صعب) = 210 سؤال إجمالي
  - الملفات: `data/questions.json`, `data/questions-part2.json`
- **الهدف**: 50 سؤال على الأقل لكل مستوى صعوبة، لكل فئة من الـ35 فئة
  - المجموع المستهدف: ~5,250 سؤال (150 سؤال/فئة)
  - الفجوة: ~5,040 سؤال جديد
- **ملاحظة**: هذا حجم عمل كبير — يُفضّل تنفيذه على دفعات (فئة فئة، أو 10 فئات بالمرة) بدل محاولة كل شيء دفعة واحدة

### 2. الرفع إلى GitHub — لم يبدأ
- [x] **تم**: تأمين المفتاح السري — أُضيف `Supabase.txt` إلى `.gitignore` (2026-07-26)، فلن يُرفع لـ GitHub. الملف نفسه ما زال موجود محلياً ولم يُعدَّل.
- [ ] `git init` في المجلد المحلي (حالياً غير مهيأ كـ git repo)
- [ ] ربطه بمستودع GitHub الموجود (`rajaxdx/raja-challenge` على الأرجح — تأكيد الاسم عند التنفيذ)
- [ ] commit + push للتحديثات

## معلومات مرجعية
- Admin PIN: `2014` (في `js/game.js`)
- Supabase Project ID: `rqcltlleqpppeywxbkpo`
- ملف قديم موجود بالمجلد: `مخمخ_رجا (5).html` (نسخة أحادية الملف قبل التقسيم — قرار معلّق: نحتفظ فيه كنسخة احتياطية أو نحذفه)

---
آخر تحديث: 2026-07-26
